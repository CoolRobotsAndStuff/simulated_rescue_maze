#include "device.hpp"

/*
template <class WebotsDevice>
void simulated_rescue_maze::Device<WebotsDevice>::update(){};
  
template <class WebotsDevice>
void simulated_rescue_maze::Device<WebotsDevice>::printValues(){};

template <class WebotsDevice>
void simulated_rescue_maze::Device<WebotsDevice>::setTimeStep(int t_timeStepMilliseconds){
    m_timeStepMilliseconds = t_timeStepMilliseconds;
    m_timeStepSeconds = t_timeStepMilliseconds / 1000;
  }
  
template <class WebotsDevice>
void simulated_rescue_maze::Device<WebotsDevice>::setDevice(WebotsDevice *t_device){
    m_device = t_device;
  }

template <class WebotsDevice>
void simulated_rescue_maze::Device<WebotsDevice>::enableDevice(int t_timeStepMilliseconds){
    m_device->enable(t_timeStepMilliseconds);
  }

template <class WebotsDevice>
void simulated_rescue_maze::Device<WebotsDevice>::disableDevice(){
    m_device->disable();
  };

template <class WebotsDevice>
void simulated_rescue_maze::Device<WebotsDevice>::initializeValues(){};
*/